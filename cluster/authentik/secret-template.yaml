# Secret: authentik-db-credentials
# Namespace: authentik
# Consumed by: CNPG Cluster (bootstrap initdb owner credentials)
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: authentik-db-credentials
#   namespace: authentik
# stringData:
#   username: authentik
#   password: <strong-random-password>

---

# Secret: authentik-secret
# Namespace: authentik
# Consumed by: Authentik HelmRelease (env vars)
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: authentik-secret
#   namespace: authentik
# stringData:
#   secret-key: <random-50-char-string>        # e.g. openssl rand -base64 36
#   AUTHENTIK_POSTGRESQL__PASSWORD: <same-as-authentik-db-credentials-password>