apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - HelmRelease.yaml
  - HelmRepository.yaml
  - webhook-repo.yaml
  - webhook-release.yaml
  - clusterissuer.yaml
