# Secret template — never commit the real secret.
# Copy this file, fill in the values, apply manually, then delete your copy.
#
#   kubectl apply -f git-backup-secret.yaml
#   rm git-backup-secret.yaml
#
# Required GitHub PAT scopes: repo (read-only is sufficient: contents:read)
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: git-backup-secret
#   namespace: dcsg2005
# stringData:
#   GITHUB_TOKEN: "<github-pat-with-repo-read>"