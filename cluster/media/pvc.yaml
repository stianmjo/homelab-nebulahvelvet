apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-nfs
  namespace: media
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: nfs-odin
  resources:
    requests:
      storage: 2Ti