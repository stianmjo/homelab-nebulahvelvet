apiVersion: v1
kind: Pod
metadata:
  name: flux-notify-test
  namespace: vaultwarden
  labels:
    app: flux-notify-test
spec:
  containers:
    - name: nginx
      image: nginx:alpine
      ports:
        - containerPort: 80
        - cS: 80
