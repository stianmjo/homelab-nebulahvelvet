apiVersion: v1
kind: Pod
metadata:
  name: flux-notify-test
  namesapce: vaultwarden
  labels:
    app: flux-notify-test
spec:
  containers:
    - name: nginx
      image: nginx:alpine
      ports:
        - containerPort: 80
